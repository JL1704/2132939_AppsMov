import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'miAppMovil-2132939',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
